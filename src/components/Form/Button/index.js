import Primary from './PrimaryButton';
import Outline from './OutlineButton';
import Transparent from './TransparentButton';

const Button = {
    Primary, 
    Outline,
    Transparent,
};

export default Button;